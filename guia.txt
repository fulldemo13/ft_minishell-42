PARTE DEL MAIN() Y DEL LAUNCH()

1)En main crear la estructura que albergara tanto la informaciión que le pase el 
usuario como el path del shell y el bin path que es lo que usará para  ejecutar
los comandos no implementados.
2)ft_comm_initialize guarda en la estructura el path, el número de lineas de este,
el path_bin y el número de comandos pasados seteado a 0.

3) crear un proceso para el shell, pero no haria falta, ya que cadaa comando del
shell debería ir en un proceso (creo).

4) Pasamos a la parte del launch:
Seteamos el file descriptor en la stdout (1), mostramos el prompt y esperamos
instrucciones con el getnext line. 
5)Comprobamos que las comillas dobles y simples no piiden multiline y almacenamos 
en number el numero de comandos pasados separados por semicolon (;) y que no haya 
puntos y comas seguidos.
6)Si pasa las chequeos hace un split pos puntos y comas y guada los stings generados
en un puntero doble, despues almacena cuantos comandos (ordenes) en una variable int.
6)Ahora ejecutamos comando a comando:
La función divider devuelve el número de palabras que tiene el comando mirando las 
comillas también.Get_commands guardas las palabras en un puntero doble.
7)Despues volvemos a partir la palabras por si hay redirecciones (< o >).
8)ft_quotes cambia la palabra que hace referencia a algo del path por su contenido
($HOME=/User/...).
9)get_fd mira si la palabra es un redireccion abre la dirección de la siguiente palabra
y guarda en la variable global_fd el nuevo fd por que tiene que mostrar el resultado
del comando

Llegados a este punto tenemos las palabras trocedas del comando y por que fd mostrar
el reultado de este.

10) Con compare miramos si alguna palabra del comando es de los comandos que tenemos
que implementar y si es así redirigimos a la función que se encarga de ellos. Si el 
comando es exit est funcion devuelve el valor de salida que toca, si es otro comando
sigue devolviendo -2.

¿Los comandos implementados deberian ejecutarse en un proceso hijo?